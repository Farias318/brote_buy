# Variables de entorno — brote_buy
# Hoy no aplica: no hay backend, todo corre client-side (localStorage + sync vía GitHub Gist,
# con el token pegado a mano desde la UI, no desde variables de entorno).
#
# Referencia para cuando se implemente el Tier 2 de ../../docs/analisis-mejoras.md
# (backend Node/TS + Postgres reemplazando el hack de Gist):
# DATABASE_URL=
# PORT=
# NODE_ENV=development
